import { zeroPad } from 'pokedex/helpers/zero-pad';
import { module, test } from 'qunit';

module('Unit | Helper | zero pad');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = zeroPad([42]);
  assert.ok(result);
});
