import { formatGen } from 'pokedex/helpers/format-gen';
import { module, test } from 'qunit';

module('Unit | Helper | format gen');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatGen([42]);
  assert.ok(result);
});
