import { pokeType } from 'pokedex/helpers/poke-type';
import { module, test } from 'qunit';

module('Unit | Helper | poke type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = pokeType([42]);
  assert.ok(result);
});
